server:
  port: 9080
  servlet:
    context-path: /appAuth


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/basejpa?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


swagger:
  is:
    enable: true
